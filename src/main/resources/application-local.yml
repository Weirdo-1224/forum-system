spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/forum}
    username: ${DB_USERNAME:forum}
    password: ${DB_PASSWORD:replace-with-local-password}
  flyway:
    enabled: true

forum:
  security:
    jwt:
      secret: ${JWT_SECRET:replace-with-at-least-32-byte-local-secret}